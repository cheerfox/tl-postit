$('#post_<%= @post.id %>_vote').html(<%= @post.total_votes %>);
<% if @post.errors.any? %>
  $('#flash_error').html("You can only vote a post once");
<% else %>
  $('#flash_notice').html("Your voting is successful!!");
<% end %>